[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python api_server.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/api/ml/model-info"
healthcheckTimeout = 100

[environments.production.variables]
PYTHONPATH = "/app"
FLASK_ENV = "production"